{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "description": "Flutter SDK plugin loader is resolved via included build, not a Maven repository",
      "matchPackageNames": [
        "dev.flutter.flutter-plugin-loader"
      ],
      "enabled": false
    }
  ]
}
