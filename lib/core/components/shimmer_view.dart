/// Legacy re-export — use `package:happy_flutter/core/ui/shimmer/shimmer.dart`
/// directly for new code.
library;

import '../ui/shimmer/shimmer.dart';

export '../ui/shimmer/shimmer.dart';

/// Backward-compatible alias for [Shimmer].
typedef ShimmerView = Shimmer;
