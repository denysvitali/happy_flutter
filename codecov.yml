coverage:
  status:
    project:
      default:
        # Don't block PRs on coverage changes
        informational: true
    patch:
      default:
        informational: true

ignore:
  - "test/**"
  - "**/*.g.dart"
  - "**/*.freezed.dart"
  - "lib/generated/**"
