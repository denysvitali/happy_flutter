# Codebase Audit — 2026-04-24

15 agents ran in parallel across independent focus areas. Consolidated findings below.

## P0 — Fix Now

| # | Issue | Area | File | Detail |
|---|-------|------|------|--------|
| 1 | **PopScope race conditions** (37.5% back button error rate) | Navigation | `sessions_screen.dart:133`, `chat_screen.dart:819`, `edit_artifact_screen.dart:119`, `voice_language_settings_screen.dart:162` | `canPop` evaluated at build time, stale on rapid back taps. `setState()` async. Nested `PopScope` with conflicting conditions. |
| 2 | **ML failures logged as warnings** — should be `logger.error` + Sentry | Error Handling | `session_ranker.dart:61,80`, `ml_platform_native.dart:47,64,98,133` | Model load/inference failures (data-loss severity) suppressed as warnings. |
| 3 | **Missing Sentry captures** — 15+ catch blocks | Error Handling | `_sync_socket.dart:194`, `_sync_messaging_rpc.dart:232`, `retry_interceptor.dart:194`, `_sync_data_machines.dart:342` | Critical sync/decrypt/retry failures logged but not sent to Sentry. |

## P1 — High Priority

| # | Issue | Area | File | Detail |
|---|-------|------|------|--------|
| 4 | **Only English locale** — CLAUDE.md says 10 locales exist, but only `app_en.arb` is present | i18n | `l10n/` | No es/fr/de/ca/it/ja/pl/pt/ru/zh translations. "Today"/"Yesterday" hardcoded in `datetime_extensions.dart`. |
| 5 | **Missing test coverage** — `ProfileNotifier`, `FriendsNotifier`, `NetworkNotifier`, `ArtifactsNotifier` have zero tests | Testing | `test/providers/` | These notifiers are untested despite being central to the app. |
| 6 | **Canonical localId lifecycle contract test missing** — ROADMAP says "In Progress" but no dedicated suite exists | Testing | None | Existing tests cover pieces but not the full optimistic→REST→socket→outbox→merge path in one test. |
| 7 | **Cold start dominated by Cronet/MMKV/Encryption** — avg 2–3s on critical path | Performance | `main.dart:69–163` | `ApiClient` NativeAdapter init (~500ms) is the single biggest cost. `Encryption.create()` blocks `syncRestore()`. `TokenStorage` read could overlap. |
| 8 | **In-flight optimistic message lost on app kill** — message not added to outbox until REST failure | Offline | `_sync_messaging_send.dart` | Kill between insert and response loses the optimistic row. Outbox entry created only in `catch` block. |
| 9 | **Hardcoded strings in dev/SFTP screens** — ~30+ strings not localized | i18n | `dev_logs_screen.dart`, `encryption_debug_screen.dart`, `sftp/` | `'Copy Error'`, `'Add Shared Directory'`, etc. |
| 10 | **`sessionGitStatusSync` is a no-op** — only works via socket events, no HTTP fallback | Sync | `_sync_data_social.dart:150` | If socket misses an event, git status is stale forever. |

## P2 — Medium Priority

| # | Issue | Area | File | Detail |
|---|-------|------|------|--------|
| 11 | **Socket echo before fetch out-of-order** | Testing | `message_deduplication_e2e_test.dart` | ROADMAP gap: socket arrives before fetch. |
| 12 | **ChatScreen send/pagination UI flows untested** | Testing | `chat_screen_test.dart` | No test for tap send, scroll up, or abort button. |
| 13 | **Message state-machine test missing** | Testing | None | `draft→sending→sent/pending/failed→merged` transitions not modeled. |
| 14 | **Crypto decrypt failures** — 27 warnings from legacy NaCl + `web_crypto.dart` missing | Crypto | `encryptor.dart:113`, `web_crypto.dart` | Misclassification of NaCl as AES (~0.4%), web platform stub. |
| 15 | **MessageCacheService has no debounce** | Storage | `message_cache_service.dart` | Unlike `_JsonMapStore` and `SettingsStorage` (500ms debounce), rapid streaming messages hammer storage synchronously. |
| 16 | **MessageCacheService doc bug** — says "200-message cache" but `_maxCachedMessages = 3000` | Storage | `message_cache_service.dart:23` | Minor inconsistency. |
| 17 | **Offline machine pre-check gap** — 120s heartbeat lag, no server-side reachability probe | Offline | `new_session_dialog.dart:58` | Machine could be offline when session starts. |
| 18 | **Golden tests don't block merges** | CI/CD | `ci.yml:181` | Visual regressions can land on main. |
| 19 | **`StandardComponentType.backButton`** — ROADMAP references it but doesn't exist in codebase | Navigation | None | Likely external Sentry label. |
| 20 | **Silent catch in model parsing** | Error Handling | `session.dart:51,62,174,181,203,210` | Masks data corruption as missing sessions. |

## P3 — Sound / Already Known

- **Provider architecture clean** — no circular deps, all disposals proper, `chatActionNotifier` truly stateless
- **Sync architecture sound** — well-engineered debouncing, leading+trailing, suspend/resume/dispose lifecycle correct
- **CI/CD healthy** — 7-job pipeline, all actions current, caching optimal
- **API client patterns solid** — Dio timeouts documented, Socket.IO reconnect correct
- **Crypto decrypt warnings are expected** — rate limiting in `CryptoSecretBox._shouldCapture()` working

## Top 5 Action Items

1. **Fix PopScope race conditions** — root cause of 37.5% back button error rate
2. **Add missing Sentry captures** — ~15 catch blocks that should call `Sentry.captureException`
3. **Create locale ARB files** — only English exists, but CLAUDE.md promises 10 locales
4. **Write ProfileNotifier/FriendsNotifier tests** — zero test coverage on critical notifiers
5. **Deflake cold start** — move `Encryption.create()` to fire-and-forget, pre-warm `TokenStorage` before `runApp()`
