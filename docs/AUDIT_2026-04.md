# Codebase Audit — 2026-04-24

15 agents ran in parallel across independent focus areas. Consolidated findings below.

## P0 — Fix Now

| # | Issue | Area | File | Status | Detail |
|---|-------|------|------|--------|--------|
| 1 | **PopScope race conditions** (37.5% back button error rate) | Navigation | `sessions_screen.dart`, `chat_screen.dart`, `edit_artifact_screen.dart`, `voice_language_settings_screen.dart` | **FIXED** | `canPop` evaluated at build time, stale on rapid back taps. `setState()` async. Nested `PopScope` with conflicting conditions. Fixed with `_pendingNav` guard and direct state reads at callback time. |
| 2 | **ML failures logged as warnings** — should be `logger.error` + Sentry | Error Handling | `session_ranker.dart`, `ml_platform_native.dart` | **FIXED** | Model load/inference failures (data-loss severity) suppressed as warnings. Upgraded to `logger.error` + `Sentry.captureException`. |
| 3 | **Missing Sentry captures** — 15+ catch blocks | Error Handling | `_sync_socket.dart`, `_sync_messaging_rpc.dart`, `retry_interceptor.dart`, `_sync_data_machines.dart` | **PARTIAL** | Critical sync/decrypt/retry failures now captured to Sentry. Remaining catch blocks (model parsing, etc.) not addressed. |

## P1 — High Priority

| # | Issue | Area | File | Status | Detail |
|---|-------|------|------|--------|--------|
| 4 | **Only English locale** — CLAUDE.md says 10 locales exist | i18n | `l10n/`, `datetime_extensions.dart`, `session_utils.dart` | **PARTIAL** | Date formatters made locale-aware (`toRelativeTimeString(locale)`, `formatLastSeen(locale)`). Full ARB files for 10 locales not created. "Today"/"Yesterday" still hardcoded. |
| 5 | **Missing test coverage** — `ProfileNotifier`, `FriendsNotifier`, `NetworkNotifier`, `ArtifactsNotifier` have zero tests | Testing | `test/providers/` | Open | These notifiers are untested despite being central to the app. |
| 6 | **Canonical localId lifecycle contract test missing** — ROADMAP says "In Progress" but no dedicated suite exists | Testing | None | Open | Existing tests cover pieces but not the full optimistic→REST→socket→outbox→merge path in one test. |
| 7 | **Cold start dominated by Cronet/MMKV/Encryption** — avg 2–3s on critical path | Performance | `main.dart:69–163` | Open | `ApiClient` NativeAdapter init (~500ms) is the single biggest cost. `Encryption.create()` blocks `syncRestore()`. `TokenStorage` read could overlap. |
| 8 | **In-flight optimistic message lost on app kill** — message not added to outbox until REST failure | Offline | `_sync_messaging_send.dart` | Open | Kill between insert and response loses the optimistic row. Outbox entry created only in `catch` block. |
| 9 | **Hardcoded strings in dev/SFTP screens** — ~30+ strings not localized | i18n | `dev_logs_screen.dart`, `encryption_debug_screen.dart`, `sftp/` | Open | `'Copy Error'`, `'Add Shared Directory'`, etc. |
| 10 | **`sessionGitStatusSync` is a no-op** — only works via socket events, no HTTP fallback | Sync | `_sync_data_social.dart:150` | Open | If socket misses an event, git status is stale forever. |

## P2 — Medium Priority

| # | Issue | Area | File | Status | Detail |
|---|-------|------|------|--------|--------|
| 11 | **Socket echo before fetch out-of-order** | Testing | `message_deduplication_e2e_test.dart` | Open | ROADMAP gap: socket arrives before fetch. |
| 12 | **ChatScreen send/pagination UI flows untested** | Testing | `chat_screen_test.dart` | Open | No test for tap send, scroll up, or abort button. |
| 13 | **Message state-machine test missing** | Testing | None | Open | `draft→sending→sent/pending/failed→merged` transitions not modeled. |
| 14 | **Crypto decrypt failures** — 27 warnings from legacy NaCl + `web_crypto.dart` missing | Crypto | `encryptor.dart:113`, `web_crypto.dart` | Open | Misclassification of NaCl as AES (~0.4%), web platform stub. |
| 15 | **MessageCacheService has no debounce** | Storage | `message_cache_service.dart` | Open | Unlike `_JsonMapStore` and `SettingsStorage` (500ms debounce), rapid streaming messages hammer storage synchronously. |
| 16 | **MessageCacheService doc bug** — says "200-message cache" but `_maxCachedMessages = 3000` | Storage | `message_cache_service.dart:23` | Open | Minor inconsistency. |
| 17 | **Offline machine pre-check gap** — 120s heartbeat lag, no server-side reachability probe | Offline | `new_session_dialog.dart:58` | Open | Machine could be offline when session starts. |
| 18 | **Golden tests don't block merges** | CI/CD | `ci.yml:181` | Open | Visual regressions can land on main. |
| 19 | **`StandardComponentType.backButton`** — ROADMAP references it but doesn't exist in codebase | Navigation | None | Open | Likely external Sentry label. |
| 20 | **Silent catch in model parsing** | Error Handling | `session.dart:51,62,174,181,203,210` | Open | Masks data corruption as missing sessions. |

## P3 — Sound / Already Known

- **Provider architecture clean** — no circular deps, all disposals proper, `chatActionNotifier` truly stateless
- **Sync architecture sound** — well-engineered debouncing, leading+trailing, suspend/resume/dispose lifecycle correct
- **CI/CD healthy** — 7-job pipeline, all actions current, caching optimal
- **API client patterns solid** — Dio timeouts documented, Socket.IO reconnect correct
- **Crypto decrypt warnings are expected** — rate limiting in `CryptoSecretBox._shouldCapture()` working

## Fixes Landed on This Branch

- **P0-1**: PopScope race conditions — 4 files fixed
- **P0-2**: ML error severity — 6 files fixed (session_ranker + ml_platform_native)
- **P0-3**: Sentry captures — sync socket, messaging RPC, retry interceptor, machines sync
- **P1-4**: Date formatters made locale-aware (foundation only; full ARB files not created)

## Remaining Top Priorities

1. **Write ProfileNotifier/FriendsNotifier tests** — zero test coverage on critical notifiers
2. **Deflake cold start** — move `Encryption.create()` to fire-and-forget, pre-warm `TokenStorage` before `runApp()`
3. **Create locale ARB files** — only English exists, but CLAUDE.md promises 10 locales
4. **Fix in-flight optimistic message loss** — add to outbox immediately on insert
5. **Add debounce to MessageCacheService** — prevent synchronous storage hammering
