#!/usr/bin/env bash

export DIRENV_WARN_TIMEOUT=20s

# `use mise` activates .mise.toml — flutter / dart / java land on
# PATH and JAVA_HOME / DART_SDK are exported the moment you `cd`
# into the repo.
#
# To silence all output, use `--quiet`.
#
# Example usage: use mise --quiet
use mise
